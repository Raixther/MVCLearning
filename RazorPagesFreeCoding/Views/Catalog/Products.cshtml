@model Catalog
@{
    ViewData["Title"] = "Список продуктов";
}
          
@if(Model == null)
{
    <h3>Каталог пуст</h3>
}      
else
    @foreach (var item in Model.GetAll())
    {
        <p>@item.Name @item.Price </p>    
    } 
}